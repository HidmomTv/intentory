-- Universal Config
Config = {}
Config.Framework = 'standalone' -- 'qbcore', 'qbox', 'standalone'

-- Pesos Base (Modificables por el dueño del servidor)
Config.MaxWeight = {
    player = 30.0,
    backpack = 20.0, -- Peso extra sumado si llevan mochila inmersiva
    stash = 100.0,
    trunk = 150.0,
    glovebox = 15.0,
    drop = 50.0 -- Suelo/Loot temporal
}
